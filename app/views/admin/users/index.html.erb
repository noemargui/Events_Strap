</br></br>
<div class="card border-warning  m-5">
  <div class="card-body text-primary">
    <ul class="card-text list-group">
      <% @users.each do |user| %>
        <li class="list-group-item">
          Prénom : <%= user.first_name %></br>
          Nom : <%= user.last_name %></br>
          Email : <%= user.email %><br>
          <center>
            <%= link_to "Afficher", admin_user_path(user.id),:class => "btn-outline-warning btn p-1"%>
            <%= link_to "Modifier", edit_admin_user_path(user.id),:class => "btn-outline-warning btn p-1"%>
            <a href="/admin/users/<%= user.id %>" class="btn btn-outline-danger p-1" data-confirm="Tu es sûr?" data-method="delete">Supprimer</a>
        </li>
      <% end %>
      </br></br>
    </ul>
  </div>
</div>

