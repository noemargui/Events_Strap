<!-- Création d'un event -->

<div class="card border-warning m-5">
 <div class="card-header">
  <center>
    <h1>🎫 Ajouter un event</h1>
  </div>
  <center><%= form_tag url_for(action: 'create'), method: "post" do %>
    <div class="card-body text-warning">
      <div class="form-group">
        <div class="form-group">
          <%= label_tag 'Title' %>
          <%= text_field_tag 'title', "#{@event.title}", :class => "form-control" %>
        </div>

        <div class="form-group">
          <%= label_tag 'Description' %>
          <%= text_field_tag 'description', "#{@event.description}", :class => "form-control", :rows => "4" %>
        </div>

        <div class="form-group">
          <%= label_tag 'Start Date' %>
          <%= date_field_tag 'start_date', "#{@event.start_date}", :class => "form-control" %>
        </div>

        <div class="form-group">
          <%= label_tag 'Duration' %>
          <%= text_field_tag 'duration', "#{@event.duration}", :class => "form-control", :placeholder => "5, 10, 15, 20 min etc..", :rows => "4" %>
        </div>

        <div class="form-group">
          <%= label_tag 'Price' %>
          <%= text_field_tag 'price', "#{@event.price}", :class => "form-control", :placeholder => "Min 1 € - Max 999 €", :rows => "4" %>
        </div>

        <div class="form-group">
          <%= label_tag 'Location' %>
          <%= text_field_tag 'location', "#{@event.location}", :class => "form-control", :rows => "4" %>
        </div>


        <br />
        <%= submit_tag "Créer mon Event", class: "btn btn-outline-warning btn-lg" %>
        <% end %></center>
      </div>
    </div>
  </div>
  <% if @event.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <p>Impossible de créer l'event pour la/les raison(s) suivante(s) :</p>
    <ul>
      <% @event.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
</center>




